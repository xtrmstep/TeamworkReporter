@model TeamworkReporter.Models.Auth.ForgetPasswordViewModel

<label for="inputEmail" class="sr-only">Email</label>
<input type="email" id="inputEmail" name="@Html.NameFor(m => m.Email)" class="form-control single" placeholder="Email" required autofocus value="@Html.ValueFor(m=> m.Email)">
@Html.ValidationMessageFor(m => m.Email)